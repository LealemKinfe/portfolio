export type param = {
  params: { project: string };
};

export type card = {
  imgSrc: string;
  title: string;
  link: string;
};

export type project = {
  name: string;
  client: string;
  about: string;
  imgUrl: string;
  caseStudy: string;
  caseStudyLink: string;
  youtubeLink: string;
  demoLink: string;
};
